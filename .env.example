# LAAD - ATM Log Aggregation, Analysis & Diagnostics Platform

# Copy this file to .env and update the values before running:
#   cp .env.example .env

# GENERATOR CONFIGURATION
# Tick interval (seconds) between emission cycles
TICK_SECONDS=1

# Historical backfill minutes on startup (0 = disabled)
BACKFILL_MINUTES=0

# Anomaly injection probability (0.002 = 0.2%)
ANOMALY_PROB=0.002

# Optional: Seed for reproducible generation
GENERATOR_SEED=


# DATABASE CONFIGURATION
POSTGRES_HOST=localhost
POSTGRES_PORT=5434
POSTGRES_DB=atm_platform
POSTGRES_USER=atm_user
POSTGRES_PASSWORD= # REQUIRED: Set your password before running

# JWT AUTHENTICATION
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY= # REQUIRED: Generate a secure secret key


# TEST DATABASE (used by pytest)
TEST_POSTGRES_HOST=postgres_test
TEST_POSTGRES_PORT=5432
TEST_POSTGRES_DB=atm_platform_test
TEST_POSTGRES_USER=atm_user
TEST_POSTGRES_PASSWORD= # REQUIRED: Same as POSTGRES_PASSWORD


# ML ANOMALY DETECTION
# Enable/disable detection layers
ML_HEURISTICS_ENABLED=true
ML_RULES_DETECTION_ENABLED=true
MLFLOW_TRACKING_URI=http://localhost:5001


# RAG DIAGNOSTIC ASSISTANT - LLM PROVIDERS
# Primary: OpenRouter (free tier) - RECOMMENDED
# Get free API key from https://openrouter.ai
OPENROUTER_API_KEY= # REQUIRED: Set your OpenRouter API key

# Fallback: Google Gemini (optional)
GEMINI_API_KEY=

# Fallback: Groq (optional)
GROQ_API_KEY=


# RAG CONFIGURATION
# LLM Models (OpenRouter format)
RAG_PRIMARY_MODEL=google/gemma-4-26b-a4b-it:free
RAG_FALLBACK_MODEL=nvidia/nemotron-nano-9b-v2:free

# Retrieval settings
RAG_TOP_K=3
RAG_SAMPLES=1
RAG_TEMPERATURE=0.6
RAG_CHUNK_TRUNCATE=200

# Confidence thresholds
CONF_HIGH=0.8
CONF_MEDIUM=0.5

# Enable Platt scaling calibration
RAG_CALIBRATION_ENABLED=true


# REDIS CACHE (for RAG response caching)
# Host: 'redis' for Docker, 'localhost' for local development
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_CACHE_TTL=300